{
  "name": "slums-launcher",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "dev": "serve src/ -l 1420 --no-clipboard --single",
    "tauri": "tauri",
    "build:frontend": "node scripts/build-frontend.js",
    "postbuild:appimage": "node scripts/patch-appimage.js",
    "build": "npm run build:frontend && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "serve": "^14.2.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1"
  }
}
